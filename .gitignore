node_modules
.env
.env.local
.env.*.local

# Deliverability test log — seed addresses and message IDs, local only
scripts/.deliverability-log.jsonl

# Extracted iPhone Messages data. Real message history must never be committed —
# the extract/parse scripts write here, and it contains customers' conversations.
scripts/iphone/data/
*.sms.db

# Wrap mockup test renders (evidence, not source)
scripts/wrap-test/
